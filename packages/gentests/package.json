{
  "name": "@clapp/gentests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CLAPP generated test suite (CLAPP-032): renders a SynthesisPlan (contract v0.1 mirror) into an executable bun-test suite that verifies a conforming candidate app — route/structure tests, mock-api tests, and journey acceptance replays — plus the minimal reference conforming server used to self-verify the generated suite.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p .",
    "test": "bun test"
  },
  "dependencies": {
    "@clapp/core": "workspace:*",
    "@clapp/journey": "workspace:*"
  }
}
