// @clapp/core — shared run/event/artifact contract (v0).
//
// NOTE: shared scaffolding owned by Worker 1 (CLAPP-001); recreated locally by
// Worker 3 (CLAPP-003) so this workspace compiles standalone. Identical copies
// are reconciled by the tech lead at harvest — the contract types are verbatim
// from the tech-lead interface declaration and must not be renamed.

export * from './contract';
